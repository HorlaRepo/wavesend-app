/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApiResponsePagedTransactionResponse } from '../../models/api-response-paged-transaction-response';

export interface SearchTransactions$Params {
  query?: string;
  sortOrder?: string;
  filter?: string;
  page?: number;
  size?: number;
}

export function searchTransactions(http: HttpClient, rootUrl: string, params?: SearchTransactions$Params, context?: HttpContext): Observable<StrictHttpResponse<ApiResponsePagedTransactionResponse>> {
  const rb = new RequestBuilder(rootUrl, searchTransactions.PATH, 'get');
  if (params) {
    rb.query('query', params.query, {});
    rb.query('sortOrder', params.sortOrder, {});
    rb.query('filter', params.filter, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApiResponsePagedTransactionResponse>;
    })
  );
}

searchTransactions.PATH = '/transactions/search';
