/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApiResponsePagedTransactionResponse } from '../../models/api-response-paged-transaction-response';

export interface GetTransactionsByWallet$Params {
  walletId: string;
  page?: number;
  size?: number;
}

export function getTransactionsByWallet(http: HttpClient, rootUrl: string, params: GetTransactionsByWallet$Params, context?: HttpContext): Observable<StrictHttpResponse<ApiResponsePagedTransactionResponse>> {
  const rb = new RequestBuilder(rootUrl, getTransactionsByWallet.PATH, 'get');
  if (params) {
    rb.path('walletId', params.walletId, {});
    rb.query('page', params.page, {});
    rb.query('size', params.size, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApiResponsePagedTransactionResponse>;
    })
  );
}

getTransactionsByWallet.PATH = '/transactions/wallet/{walletId}';
