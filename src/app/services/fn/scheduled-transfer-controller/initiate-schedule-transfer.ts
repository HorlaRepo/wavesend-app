/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApiResponseScheduledTransferInitiationResponse } from '../../models/api-response-scheduled-transfer-initiation-response';
import { ScheduledTransferRequestDto } from '../../models/scheduled-transfer-request-dto';

export interface InitiateScheduleTransfer$Params {
      body: ScheduledTransferRequestDto
}

export function initiateScheduleTransfer(http: HttpClient, rootUrl: string, params: InitiateScheduleTransfer$Params, context?: HttpContext): Observable<StrictHttpResponse<ApiResponseScheduledTransferInitiationResponse>> {
  const rb = new RequestBuilder(rootUrl, initiateScheduleTransfer.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApiResponseScheduledTransferInitiationResponse>;
    })
  );
}

initiateScheduleTransfer.PATH = '/scheduled-transfers/initiate';
