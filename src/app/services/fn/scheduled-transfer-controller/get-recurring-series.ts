/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { ApiResponseListScheduledTransferResponseDto } from '../../models/api-response-list-scheduled-transfer-response-dto';

export interface GetRecurringSeries$Params {
  parentId: number;
}

export function getRecurringSeries(http: HttpClient, rootUrl: string, params: GetRecurringSeries$Params, context?: HttpContext): Observable<StrictHttpResponse<ApiResponseListScheduledTransferResponseDto>> {
  const rb = new RequestBuilder(rootUrl, getRecurringSeries.PATH, 'get');
  if (params) {
    rb.path('parentId', params.parentId, {});
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<ApiResponseListScheduledTransferResponseDto>;
    })
  );
}

getRecurringSeries.PATH = '/scheduled-transfers/recurring/{parentId}';
